<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.mapper.UserInsertMapper">
    <insert id="selUserInsert" parameterType="long">
    	insert into T_USER_INFO (USER_NAME, USER_ID, USER_PW, USER_EMAIL)
    	values ('100','2','3','4')
	    <!-- SELECT USER_NAME, USER_ID, USER_PW, USER_EMAIL
	    FROM T_USER_INFO WHERE USER_NAME='lwhathamrr' --> 
        <!-- select 
			USER_NAME,
			USER_ID,
			USER_PW,
			USER_EMAIL
		from t_user_info
		LIMIT 0,1 -->
    </insert>
</mapper>
